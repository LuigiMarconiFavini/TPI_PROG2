Activar entorno en el terminal
source .venv/bin/activate
pip install requests <- instala todas las librerias necesarias

export FLASK_APP=app.py <- informa interfaz de línea de comandos (CLI) de Flask dónde encontrar tu aplicación principal de Flask.

flask init-db <- inicializa la base de datos, se cargan un admin y un cliente
Base de datos y tablas creadas (o ya existentes).
Productos iniciales cargados con stock.
Usuario administrador 'admin@frutales.com' creado con contraseña 'admin123'.
Usuario cliente 'cliente@frutales.com' creado con contraseña 'cliente123'.

flask run <- arranca la app, van a ver que en el terminal les da una ip como direccion, la corren y se abre en el navegador (live server no funciona con flask)